#include "uv.hh"
#include <iostream>
/* WARNING: THIS FILE IS CURRENTLY ******DEPRECATED****** */
/*
UVSocketServer::UVSocketServer() {
  // placeholder for now
}

UVSocketServer::~UVSocketServer() {
  // placeholder for now
}

bool UVSocketServer::init(const char *host, int port, uv_loop_t *_loop) {
  uv_tcp_init(loop, &server);
  uv_ip4_addr(host, port, &addr);
  uv_tcp_bind(&server, (const sockaddr*)&addr, 0);

  int r = 0;
  if (r) {
    std::cerr << "Error listening: " << uv_strerror(r) << "\n";
    return false;
  }
  return true;
}
*/
