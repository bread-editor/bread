#include <uv.h>
#include <functional>
/* WARNING: THIS FILE IS CURRENTLY *******DEPRECATED******** */
/* This file contains some wrapping for LibUV for easier use with C++11 */
/*
class UVSocketServer {
public:
  bool init(const char *host, int port, uv_loop_t *_loop);
  
  UVSocketServer();
  ~UVSocketServer();

private:
  uv_tcp_t server;
  sockaddr_in addr;
  uv_loop_t *loop;
};
*/
