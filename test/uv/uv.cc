#include <uv/uv.hh>
#include <gtest/gtest.h>

/*
UVSocketServer test;
uv_loop_t *loop;

TEST(UV, InitializeUVSocketServer) {

  loop = uv_default_loop();
  ASSERT_EQ(true, test.init("127.0.0.1", 8909, loop));
}

TEST(UV, AcceptConnection) {
  
}
*/
