test:
  pre:
    - go get github.com/axw/gocov/gocov
    - go get github.com/mattn/goveralls
  override:
    - gocov test ./... > gocov.json
    - gocov report gocov.json
  post:
    - /home/ubuntu/bin/goveralls -gocovdata=gocov.json -service="circle-ci"