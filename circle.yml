test:
  pre:
    - go get github.com/axw/gocov/gocov
    - go get github.com/mattn/goveralls
  override:
    - gocov test ./... > gocov.json
    - gocov report gocov.json
  post:
    - goveralls -gocovdata=gocov.json -service="circle-ci" -repotoken=$COVERALLS_TOKEN