machine:
  ghc:
    version: 7.10.1

test:
  post:
    - cabal haddock
    - cp ./dist/build/bread/bread $CIRCLE_ARTIFACTS/
    - cp ./dist/build/libHSbread-* $CIRCLE_ARTIFACTS/
    - zip -r $CIRCLE_ARTIFACTS/bread-doc.zip ./dist/doc/html/bread

general:
  artifacts:
