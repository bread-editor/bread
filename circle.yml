machine:
  ghc:
    version: 7.10.1

test:
  post:
    - cabal haddock
    - cp ./build/bread/bread $CIRCLE_ARTIFACTS/
    - zip ./build/doc/html/* $CIRCLE_ARTIFACTS/bread-doc.zip

general:
  artifacts:
