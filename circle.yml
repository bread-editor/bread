machine:
  ghc:
    version: 7.10.1

test:
  post:
    - cabal haddock
    - cp ./dist/bread/bread $CIRCLE_ARTIFACTS/
    - zip ./dist/doc/html/* $CIRCLE_ARTIFACTS/bread-doc.zip

general:
  artifacts:
