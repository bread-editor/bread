package tcp_server_test

import (
	//"fmt"
	//"github.com/bread-editor/bread/tcp_server"
	//"net"
	"testing"
)

func TestServer(t *testing.T) {
	go func() {
		//tcp_server.StartTCPServer(nil, nil)
	}()

	//conn, _ := net.Dial("tcp", ":8488")
	//conn.Write([]byte("hello"))

	//buf := make([]byte, 512)
	//conn.Read(buf)
	//fmt.Printf("%v", buf)
	//conn.Close()
}
